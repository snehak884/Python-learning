# a = (3, 2, 22, 13)

# print(a)
# print(a[2])
# a[3] = 32

# b = (3, )


mixed_tuple = (1, "hello", 3.14, True)
print(mixed_tuple)     # (1, 'hello', 3.14, True)


# Nested Tuple (tuple inside tuple)

nested_tuple = ("Python", (1, 2, 3), ["a", "b", "c"])
print(nested_tuple)    # ('Python', (1, 2, 3), ['a', 'b', 'c'])
print(nested_tuple[0]) # Python
print(nested_tuple[1]) # (1, 2, 3)
